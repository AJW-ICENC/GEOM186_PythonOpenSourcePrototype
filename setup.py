# Complete

"""


from setup import setup, find_packages

setup(
    name = "ICENC_Overlap_Tools",
    version = "1.0.0",
    packages = find_packages(),
    install_requires = [
        "numpy",
        "pandas"
        # complete
    ],
    author = "Alex Wallage",
    author_email = "alex.wallage@ic-enc.org",
    description = "IC-ENC Overlaps Toolbox (expand)",
    long_description = open("README.md").read(),
    long_description_content_type = "text/markdown",
    url = "(Expand)",
    classifiers = [
        "Programming Language :: Python :: 3",
        "License :: (Expand)",
        "Operating System :: OS independent"
    ],
    python_requires = ">=3.7"
)


"""